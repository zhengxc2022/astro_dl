# Core dependencies
flask>=2.0.0
numpy>=1.20.0
astropy>=5.0.0
requests>=2.25.0
wget>=3.2

# Astronomy tools
astroquery>=0.4.0
pyvo>=1.3
beautifulsoup4>=4.10.0

# VLASS dependencies (required for VLASS downloads)
reproject>=0.9.0
radio-beam>=0.3.0

# Optional: for VLASS downloads (if not using local copy)
# Install from: https://github.com/ygordon/VLASS_cutouts
# pip install git+https://github.com/ygordon/VLASS_cutouts.git
